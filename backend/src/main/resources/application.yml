spring:
  datasource:
    url: jdbc:sqlite:kanban.sqlite3
    driver-class-name: org.sqlite.JDBC
  sql:
    init:
      mode: always
  session:
    timeout: 8h
server:
  port: 8080
kanban:
  seed-admin:
    username: admin
    password: admin123
    display-name: 超级管理员
